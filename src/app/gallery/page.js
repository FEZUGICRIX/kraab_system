import GalleryClient from './GalleryClient';

export const metadata = {
  title: 'Gallery',
  description:
    'toimisto lääkärin ravintola käytävä olohuone makuuhuone lastenhuone',
};

const Gallery = () => {
  return <GalleryClient />;
};

export default Gallery;
