import BasketPaymentClient from './BasketPaymentClient';

export const metadata = {
  title: 'Basket payment',
  description: '',
};

const BasketPayment = () => {
  return (
    <>
      <BasketPaymentClient />
    </>
  );
};

export default BasketPayment;
